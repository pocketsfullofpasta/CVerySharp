using System;

namespace Day2
{
    class Program
    {
        static void Main(string[] args)
        {
           // int age = Convert.ToInt32(Console.ReadLine());
           // string label = (age >= 18 ? "Adult" : "Underage");
           //Console.WriteLine(label);
        
        
        }
    }
}
